api-gateway :
  build: ./api-gateway
  container_name: api-gateway
  ports:
    - "8080:8080"
  env_file:
    - ./api-gateway/.env
  depends_on:
    - user-service
    - product-service 
  networks:
    - ecommerce-network